/**
 * Заглушка закрытых модулей для публичного парсера (BL_RAW_ONLY=1).
 * Оценку BIF и закрытую аналитику считает приватная сторона (Cloud Functions).
 * Любое обращение здесь — ошибка конфигурации (значит BL_RAW_ONLY не выставлен).
 */
"use strict";
module.exports = new Proxy(
  {},
  {
    get() {
      throw new Error(
        "Закрытый модуль недоступен в публичном парсере. Запускайте с BL_RAW_ONLY=1 — BIF считает приватная сторона."
      );
    },
  }
);
